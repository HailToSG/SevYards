<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<configuration xmlns="https://cm5.intertrust.ru/config" xmlns:act="https://cm5.intertrust.ru/config/action">

    <collection name="ter_yard_collection" idField="id">
        <prototype>
            <![CDATA[
            SELECT
              terr.id               AS id,
              terr.name             AS name,
              terr.number           AS number,

                    (SELECT yard.objects_area
                    FROM ter_yard yard
                    WHERE yard.territory_id = terr.id)
                    AS objects_area,

                    (SELECT yard.objects_area
                    FROM ter_yard yard
                    WHERE yard.territory_id = terr.id)
                    AS summary_area,

                    (SELECT COUNT(*) FROM inventory
                    WHERE terr.id = territory_id)
                    AS inventories

            FROM ter_territory terr
            WHERE
              terr.territory_type='Дворовая территория' ::where-clause
            ]]>
        </prototype>

    </collection>

    <collection-view name="ter_yard_collection_view" collection="ter_yard_collection" is-default="true">
        <display>
            <column field="name" name="Имя" type="string" editable="false"
                    sortable="true" min-width="50px" text-break-style="cut"/>
            <column field="number" name="Номер" type="string" editable="false"
                    sortable="true" min-width="50px" text-break-style="cut"/>
            <column field="objects_area" name="Площадь зданий, сооружений, строений" type="integer" editable="false"
                    sortable="true" min-width="50px" text-break-style="cut"/>
            <column field="summary_area" name="Общая площадь" type="integer" editable="false"
                    sortable="true" min-width="50px" text-break-style="cut"/>
            <column field="inventories" name="Инвентаризации" type="integer" editable="false" sortable="true"
                    min-width="50px" text-break-style="cut">
                <child-collection-viewer filter="byTerritory" domain-object-type-to-create="inventory"
                                         bread-crumb="Инвентаризации">
                    <collection-viewer>
                        <collection-ref name="inventory_collection"/>
                        <act:tool-bar useDefault="false"/>
                    </collection-viewer>
                </child-collection-viewer>
            </column>
        </display>
    </collection-view>

</configuration>