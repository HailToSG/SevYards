<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<configuration xmlns="https://cm5.intertrust.ru/config" xmlns:act="https://cm5.intertrust.ru/config/action">

    <collection name="ter_public_collection" idField="id">
        <prototype>
            <![CDATA[
            SELECT
              territory.id                   AS id,
              territory.kind_of_territory    AS kind,
              territory.summary_area         AS sum_area,
              territory.objects_area         AS obj_area

            FROM ter_public territory
            WHERE
              1=1 ::where-clause
            ]]>
        </prototype>

    </collection>

    <collection-view name="ter_public_collection_view" collection="ter_public_collection" is-default="true">
        <display>
            <column field="kind" name="Вид общественной территории" type="string" editable="false" sortable="true"
                    min-width="50px" text-break-style="cut"/>
            <column field="sum_area" name="Общая площадь" type="integer" editable="false"
                    sortable="true" min-width="50px" text-break-style="cut"/>
            <column field="obj_area" name="Площадь зданий, сооружений, строений" type="integer" editable="false"
                    sortable="true" min-width="50px" text-break-style="cut"/>
        </display>
    </collection-view>

</configuration>